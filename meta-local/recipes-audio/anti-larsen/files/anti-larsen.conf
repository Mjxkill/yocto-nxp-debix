# anti-larsen V15 — conf du daemon (détection + décision).
# L'actuation = notchs logiciels mixer-pro (ops larsen_*), JAMAIS le TAC.
# enable = 0 : OFF par défaut (invariant : automation non validée = opt-in),
# activable en live depuis la GUI (page SYSTÈME) ou : printf "enable 1" | socat - UNIX-CONNECT:/run/anti-larsen.sock
enable = 0
thresh_db = -45
pnr_db = 25
persist_n = 4
depth_start_db = -12
depth_step_db = -3
verdict_ms = 300
refine_ms = 400
confirm_drop_db = 15
in_floor_db = -70
blacklist_s = 10
release_s = 60
